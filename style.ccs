:root {
    --bg-header: #8B008B;
    --bg-main: #720041;
    --bg-aside: #f0f0f0;
    --text-color: #000;
}

body.dark-mode {
    --bg-header: #1a1a1a;
    --bg-main: #2b2b2b;
    --bg-aside: #333333;
    --text-color: #ffffff;
}

* { margin: 0; padding: 0; box-sizing: border-box; }
body { font-family: 'Lucida Sans', sans-serif; background-color: #f0f0f0; overflow-x: hidden; }

.header {
    background-color: var(--bg-header);
    width: 100%; height: 81px;
    display: flex; justify-content: center; align-items: center;
    color: white; position: relative;
}

.dark-mode-btn {
    position: absolute; right: 20px;
    padding: 8px 15px; cursor: pointer; border-radius: 20px; border: none;
}

.main-container { display: flex; min-height: calc(100vh - 81px); }

.starlight {
    background-color: var(--bg-aside); width: 350px; padding: 20px;
    border-right: 5px solid black; display: flex; flex-direction: column; align-items: center;
}

.side-menu { flex: 1; background-color: var(--bg-main); padding: 40px; }

.gallery-grid {
    display: grid; grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
    gap: 25px; padding: 20px;
}

.book-item { position: relative; cursor: pointer; transition: 0.3s; }
.book-item img { width: 100%; border-radius: 5px; height: 280px; object-fit: cover; }
.book-item:hover { transform: translateY(-10px); }

.fav-btn {
    position: absolute; top: 10px; right: 10px;
    background: rgba(0,0,0,0.7); color: gold; border: 1px solid gold;
    border-radius: 50%; width: 35px; height: 35px; cursor: pointer;
}


.info-side-panel {
    position: fixed; right: -100%; top: 0; width: 450px; height: 100vh;
    background: #1a1a1a; color: white; transition: 0.4s; z-index: 2000;
    padding: 25px; box-shadow: -10px 0 30px rgba(0,0,0,0.8); overflow-y: auto;
}
.info-side-panel.active { right: 0; }

.panel-main-info img { width: 150px; height: 220px; object-fit: cover; border-radius: 8px; }
.author-label { color: #aaa; margin: 10px 0; }
.read-now-btn { 
    background: var(--bg-header); color: white; border: none; 
    padding: 12px 25px; border-radius: 5px; cursor: pointer; margin-top: 20px; 
}


.reading-overlay {
    position: fixed; top: 0; left: 0; width: 100%; height: 100%;
    background: black; z-index: 3000; display: flex; flex-direction: column;
}
.reading-nav { display: flex; justify-content: space-between; padding: 15px; background: #111; align-items: center; }
.pdf-frame-container { flex-grow: 1; }
#pdf-viewer { width: 100%; height: 100%; }
.hidden { display: none !important; }

.chapter-item {
    padding: 15px; background: #252525; margin-bottom: 8px; border-radius: 8px;
    cursor: pointer; list-style: none;
}
.chapter-item:hover { background: #333; }
